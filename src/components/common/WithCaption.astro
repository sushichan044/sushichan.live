---
export type CaptionProps = {
  isFigure?: boolean
  caption?: string
}

type Props = CaptionProps

const { isFigure, caption } = Astro.props
const CaptionTag = isFigure ? "figcaption" : "span"
---

<slot />
{
  caption && (
    <CaptionTag class="justify-center text-center text-base">
      {caption}
    </CaptionTag>
  )
}
